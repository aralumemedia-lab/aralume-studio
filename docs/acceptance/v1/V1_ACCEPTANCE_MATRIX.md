# Matriz de aceite V1.0 — Sprint 14

## Identificação e regra

- Data da execução: 2026-07-15.
- PR: #24 — `https://github.com/aralumemedia-lab/aralume-studio/pull/24`.
- Branch: `codex/sprint-14-v1-acceptance`.
- Base normativa: `main`/`origin/main` em `d78959a47a2bafbb343408d703eafafec8c6df59`.
- Commit-base das evidências: `22388e7`.
- Head vigente da PR: deve ser confirmado diretamente no GitHub no momento da revisão; não é fixado nesta matriz.
- Spec: [`docs/specs/012-v1-acceptance.md`](../../specs/012-v1-acceptance.md).
- Critérios oficiais: [`docs/PROJECT_MASTER.md`](../../PROJECT_MASTER.md), V1-01 a V1-18.
- Regra: todos os 18 critérios obrigatórios precisam estar `PASS` para aceitar a V1.0.

Os resultados desta matriz foram produzidos no commit-base `22388e7`. Correções documentais posteriores não alteram os resultados funcionais, salvo quando explicitamente registrado.

## Resultado

`V1.0 NÃO ACEITA`

| ID | Critério e descrição | Rota / ação do operador | Endpoint ou contrato | Dados / IDs | Resultado esperado | Resultado observado | Evidência | Status | Lacuna / severidade | Recomendação / backlog |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| V1-01 | Criar ou selecionar canal e manter contexto isolado. | `/channels`; selecionar um canal e navegar com o contexto ativo. | `GET/POST/PATCH /api/channels`; `Channel`. | `ch_historia`, `ch_curiosidades`, `ch_negocios`; nenhum ID novo nesta rodada. | Canal real selecionado/criado e `channelId` preservado. | Lista real renderizada, seleção/contexto cobertos por rota e testes; criação não foi necessária para satisfazer “criar ou selecionar”. | E-01, E-02; `src/routes/channels.tsx`; testes de canais. | PASS | Revalidar no fluxo completo; LOW. | Cobrir novamente após remediação; sem item novo. |
| V1-02 | Configurar perfil editorial. | `/channels`; abrir configuração e salvar tom, idioma, formato, audiência e regras. | `GET/PATCH /api/channels/:id`; `Channel`/`ChannelSettings`. | Canal ativo; nenhum perfil novo persistido. | Formulário completo, validação e persistência após reload. | O perfil é exibido, mas não foi comprovado fluxo frontend completo de configuração e persistência. | E-06; `src/routes/channels.tsx`; contratos em `docs/FRONTEND_API_CONTRACTS.md`. | FAIL | Mutação de perfil ausente/não comprovada; HIGH. | R14-02 — história funcional P1. |
| V1-03 | Criar pauta/ideia e iniciar transição editorial. | `/ideas`; clicar `Nova pauta`, preencher e enviar; depois pesquisar/priorizar. | `POST/PATCH /api/content-ideas`; `ContentIdea`. | Seed `idea_01`, `idea_02`, `idea_06`; nenhum ID novo. | Nova pauta persistida e transições observáveis. | A lista real carregou, mas `Nova pauta` não abriu formulário/mutation; `Pesquisar` e `Priorizar` exibiram toasts `mockado`. | E-05; `src/routes/ideas.tsx:76,85`; browser local. | FAIL | Caminho obrigatório ausente e ações mockadas; BLOCKER. | R14-03 — história funcional P1. |
| V1-04 | Registrar pesquisa, fontes e claims rastreáveis. | `/research`; criar sessão, adicionar fonte e registrar claim/evidência. | `POST /api/research-sessions`; `POST /api/research-sessions/:id/sources`; `POST /api/research-sessions/:id/claims`. | `rs_01`, `rs_02`; nenhum source/claim novo. | Sessão, URL/origem, claim e vínculo com pauta persistidos. | `/research` somente consultou sessões; não houve registro frontend de fonte/claim. | E-06; rota e contratos; testes de serviços não são prova E2E. | FAIL | Leitura sem comando operacional; HIGH. | R14-04 — história funcional P1. |
| V1-05 | Criar roteiro versionado. | `/scripts`; criar roteiro, salvar versão e consultar histórico. | `POST/PATCH /api/scripts`; `GET/POST /api/scripts/:id/versions`. | `sc_01`; nenhuma versão nova. | Roteiro e versões persistidos, com histórico navegável. | `/scripts` apenas listou roteiros; criação/versionamento não foi demonstrado pelo frontend. | E-06; `src/routes/scripts.tsx`; contratos. | FAIL | Fluxo de criação/versionamento ausente na prova; HIGH. | R14-05 — história funcional P1. |
| V1-06 | Planejar cenas/plano visual. | `/production`/detalhe editorial; criar plano e adicionar cenas. | `POST/PATCH /api/visual-plans`; `POST /api/visual-plans/:id/scenes`. | Nenhum `vp_*` ou `scene_*` novo. | Plano visual e cenas vinculados ao roteiro/pauta. | Há contratos/backend, mas nenhuma tela frontend operacional de criação/edição foi comprovada. | E-03, E-06; `src/services/visual-plans-api.ts`; contratos. | NOT PROVEN | Ausência de prova de comando visual; HIGH. | R14-06 — história funcional P1. |
| V1-07 | Registrar ou usar narração própria/autorizada. | `/media-assets`; registrar narração, origem, autorização e vínculo. | `POST/PATCH /api/media-assets`; `MediaAssetBase`. | `ma_hist_narration_01` é seed; nenhum ativo novo. | Origem/licença/autorização e uso persistidos. | Seed de narração existe, mas a entrada/validação frontend não foi comprovada. | E-03, E-06; API de media assets; dados seed. | NOT PROVEN | Seed não prova operação do operador; HIGH. | R14-07 — história funcional P1. |
| V1-08 | Registrar e vincular ativos visuais rastreáveis. | `/media-assets`; importar/registrar ativo e validar procedência/integridade. | `POST/PATCH /api/media-assets`; validação de storage/integridade; `MediaAssetBase`. | Quatro assets seed; nenhum ID novo. | Asset persistido com origem, licença/prompt/modelo, checksum e vínculo. | A tela lista/detalha, mas registro/importação frontend completo não foi demonstrado. | E-03, E-06; `src/routes/media-assets.tsx`; contratos. | FAIL | Comando de criação/procedência não comprovado; HIGH. | R14-08 — história funcional P1. |
| V1-09 | Iniciar e acompanhar render controlado. | `/videos`; selecionar assets e iniciar render, acompanhar estados. | `POST /api/renders`; `GET /api/renders`, `/api/videos`; `RenderJob`. | Seeds de asset/render; nenhum job novo no E2E. | Job, vídeo, custo, auditoria e estados persistidos. | A rota expõe controles reais e testes passam, mas nenhum render foi executado no E2E desta aceitação. | E-02, E-06; `src/routes/videos.tsx`; contratos de render. | NOT PROVEN | Falta transação observada; HIGH. | R14-09 — história funcional P1. |
| V1-10 | Criar e rastrear corte derivado. | `/clips`; selecionar vídeo-mãe, informar intervalo e criar corte. | `POST /api/clips`; `GET /api/clips/:id`; `DerivedClip`. | Nenhum `clip_*` novo nesta rodada. | Corte vinculado, arquivo, gancho, duração e status persistidos. | Contrato/serviço existem, mas o fluxo E2E de criação e persistência não foi concluído. | E-02, E-06; `src/routes/clips.tsx`; contratos de cortes. | NOT PROVEN | Falta criação observada; HIGH. | R14-10 — história funcional P1. |
| V1-11 | Validar qualidade técnica e bloquear avanço quando necessário. | `/approvals`; consultar quality check e validar resultado. | `GET/POST /api/quality-checks`; `QualityCheck`. | Nenhum `qc_*` novo; `qc_1` aparece apenas em testes. | Resultado técnico visível, bloqueio e correção auditáveis. | Validações existem no backend/fluxo de aprovação, sem demonstração frontend completa. | E-02, E-06; `src/routes/approvals.tsx`; testes de governance. | NOT PROVEN | Gate de qualidade não demonstrado; HIGH. | R14-11 — história funcional P1. |
| V1-12 | Validar conformidade e direitos. | `/compliance`/`/approvals`; consultar alerta material e revalidar. | `GET/POST /api/compliance-checks`; `ComplianceCheck`. | Nenhum `cc_*` novo; `cc_1` aparece apenas em testes. | Risco bloqueante impede avanço até correção. | Alertas reais são consultáveis, mas o cenário negativo e o bloqueio não foram exercitados pelo frontend. | E-02, E-06; rotas de compliance/approval; testes de governance. | NOT PROVEN | Bloqueio material não demonstrado; HIGH. | R14-12 — história funcional P1. |
| V1-13 | Submeter e decidir aprovação humana. | `/approvals`; aprovar, rejeitar ou solicitar mudanças e consultar histórico. | `GET /api/approvals`; `POST /api/approvals/:id/{approve,reject,request-changes}`. | Nenhuma decisão nova; `ap_2` aparece em testes. | Decisão humana persistida, bloqueio/avanço e auditoria visíveis. | Ações reais existem e têm testes, mas não houve decisão humana E2E nesta execução. | E-02, E-06; `src/routes/approvals.tsx`; testes de governance. | NOT PROVEN | Falta ação de operador observada; HIGH. | R14-13 — história funcional P1. |
| V1-14 | Preparar publicação assistida ou rascunho autorizado. | `/publications`; criar pacote/job, consultar readiness e manter sem upload externo. | `POST /api/publications`; targets/readiness; `PublicationJob`. | Nenhum job novo; destinos seed. | Pacote auditável, readiness e bloqueios visíveis, sem envio automático. | Contratos/telas existem, mas job/readiness não foram demonstrados ponta a ponta nesta rodada. | E-02, E-06; `src/routes/publications.tsx`; publicação assistida. | NOT PROVEN | Fluxo final não comprovado; HIGH. | R14-14 — história funcional P1. |
| V1-15 | Registrar custos por canal/etapa. | `/costs`; consultar resumo/breakdown por canal e período. | `GET/POST /api/costs`; summary/breakdown; `CostEntry`. | Dados seed do canal ativo; nenhum custo novo. | Custos reais/identificados por canal, etapa e período. | `/costs`, API, isolamento e testes passaram; dados operacionais foram exibidos. | E-02, E-06; rota/API de custos. | PASS | Revalidar no fluxo novo; LOW. | Sem remediação específica; cobrir no reaccept. |
| V1-16 | Registrar/consultar métricas persistidas. | `/metrics`; consultar por canal, período e conteúdo; registrar quando aplicável. | `GET/POST /api/metrics`; summary; `PerformanceMetric`. | Dados E14 com origem explícita; nenhum snapshot novo. | Métricas persistidas, canal-scoped, procedência visível. | Rota/API e testes de métricas passaram; evidência E14 está no mesmo baseline de aplicação. | E-02, E-08; `src/routes/metrics.tsx`; spec 014. | PASS | Revalidar vínculo com novo conteúdo; LOW. | Sem remediação específica; cobrir no reaccept. |
| V1-17 | Exibir recomendação editorial determinística e explicável. | `/metrics`; consultar resumo e evidências sem aplicação automática. | `GET /api/metrics/summary`; `MetricsSummary`, regra `metrics-learning-v1`. | Recomendação seed/determinística; nenhum ID novo. | Recomendação com baseline, confiança, evidências e limitações. | Regra local, testes e tela de recomendação presentes; aplicação automática não ocorre. | E-02, E-08; spec 014; rota/API de métricas. | PASS | Revalidar com conteúdo produzido; LOW. | Sem remediação específica; cobrir no reaccept. |
| V1-18 | Consultar histórico operacional completo no frontend. | `/audit-logs`; filtrar por canal e observar eventos, ator, status, custo e erro. | `GET /api/audit-logs`; `AuditLog`. | Eventos seed e históricos do canal; nenhum evento novo. | Histórico consultável, canal-scoped e rastreável. | Tabela de auditoria e API renderizaram dados; testes passaram. | E-02, E-06; `src/routes/audit-logs.tsx`; testes de audit. | PASS | Fluxos faltantes ainda não geram eventos novos; LOW. | Sem remediação específica; cobrir no reaccept. |

## Contagem e decisão

- `PASS`: 5 — V1-01, V1-15, V1-16, V1-17, V1-18.
- `FAIL`: 5 — V1-02, V1-03, V1-04, V1-05, V1-08.
- `NOT PROVEN`: 8 — V1-06, V1-07, V1-09, V1-10, V1-11, V1-12, V1-13, V1-14.
- `NOT APPLICABLE`: 0.
- Critérios obrigatórios não atendidos: 13.

Os critérios `FAIL` ou `NOT PROVEN` impedem o aceite. Endpoints, testes unitários, seeds e gates técnicos não substituem a prova operacional pelo frontend exigida pela spec.
